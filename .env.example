# === REQUIRED (never in Admin UI) ===
FLASK_SECRET_KEY=supermarket-secret-key-change-me

# === OPTIONAL infrastructure overrides (default: HTTP=80, HTTPS=443) ===
# DATABASE_URL=sqlite:////app_root/app/data/supermarkt.db
# HTTP_PORT=80
# HTTPS_PORT=443

# === BOOTSTRAP DEFAULTS (only used on first startup, then managed via Admin UI) ===
# SHOP_NAME=Kinder-Markt
# ADMIN_PIN=1234
# PRINTER_DEVICE=/dev/usb/lp0
# DEV_MODE=true
